<div class="container">
  <div class="home-content">
    <div>
      <h1>When do you need a Chef?</h1>

      <a href="http://localhost:3000/chefs/search?date=<%= DateTime.now.strftime("%d-%m-%Y") %>" class="when-links">Today</a>

      <a href="http://localhost:3000/chefs/search?date=<%= (DateTime.now + 1).strftime("%d-%m-%Y") %>" class="when-links">Tomorrow</a>

      <form action="chefs/search">
        <input type="date" name="date" placeholder="when you need a chef?" class="index-search-bar">
        <input type="submit" value="Search">
      </form>

    </div>
  </div>
</div>
